describe('getPixelsOverflowingContainer Module', () => {
  it('should give me the amount of pixelsoverflowing', () => {
    // arrange
    const testContainerSize = {
      width: 500,
      height: 200
    }

    const testImageSize = {
      width: 600,
      height: 100
    }

    // act
  })
})
